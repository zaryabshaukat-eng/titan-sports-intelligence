# Infrastructure

Phase 2.2 uses the root `docker-compose.yml` for local PostgreSQL, Redis, and the TITAN Core API. Future infrastructure-as-code, monitoring, backup, and deployment assets belong in this directory.
